## 1.0.2

- Make `fluttrim_cli` publishable on pub.dev (hosted `fluttrim_core` dependency + global executable).
- Return JSON output for `apply --json` no-op runs for CLI automation consistency.

## 1.0.0

- Initial version.
